# LocalClaw: LiteLLM proxy.
# Derived from the upstream image so that customizing is an edit here, not a
# new build system. Add RUN, COPY or ENV instructions below the FROM line and
# rebuild by re-running `lclaw up` or the manual procedure in
# docs/how-to/apply-a-deployment-by-hand.md. The digest pins the exact image;
# a LocalClaw release bumps it.
FROM ghcr.io/berriai/litellm:v1.83.14-stable@sha256:c81eb79cd4333c6cfe374c0ec929110fd23f0ee5f7fd198855a6fbddc77b83ba
